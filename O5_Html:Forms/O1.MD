## Forms intro 

~~~
input - john wick 
book my show 

form is used to collect the information and also take the input also

and that input can be processed in the backend and will be collected

and to submit we use the form tag 

collection of inputs and processing is done via form tag 
~~~

~~~
What is the usecase of forms ? 

it collects all the inputs present inside of a form html element and sends it to the 
backend for processing of the inputs 
~~~

~~~
input type="submit"
this takes the input of all the input elements inside of a form element and sends it to the backend 


~~~